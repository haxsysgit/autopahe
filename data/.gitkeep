# This file ensures the data directory structure is preserved in git
# The data/ directory itself is excluded by .gitignore to protect user privacy
# but this .gitkeep file allows the empty directory structure to exist
